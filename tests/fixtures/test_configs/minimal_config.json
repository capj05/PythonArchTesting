{
    "project": {
        "name": "minimal_config",
        "version": "0.1.0",
        "description": "Minimal configuration for testing"
    },
    "validation": {
        "rules": [
            {
                "name": "no_circular_imports",
                "type": "import_analysis",
                "enabled": true
            }
        ]
    },
    "reporting": {
        "format": "json",
        "output": "minimal_report.json"
    }
}